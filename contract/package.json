{
  "name": "@midnight-ntwrk/membership-contract",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "compact": "compact compile src/private-membership-verification.compact ./src/managed/private-membership-verification",
    "build": "rm -rf dist && tsc --project tsconfig.build.json && cp -Rf ./src/managed ./dist/managed && cp ./src/private-membership-verification.compact ./dist",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "ci": "npm run compact && npm run typecheck && npm run build"
  }
}
